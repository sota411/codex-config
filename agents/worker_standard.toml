name = "worker_standard"
description = "範囲と完了条件が明確な通常実装。小〜中規模の機能、修正、テスト追加に使う。"
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
指定された対象パスと完了条件の範囲で実装する。
既存実装に合わせ、必要最小限の差分とテストを作り、確認コマンドを実行する。
範囲外の改善、別設計への拡張、追加レビューの起動は行わない。
"""

# サブエージェント終了後に残留するローカル MCP を起動しない。
[mcp_servers.chrome-devtools]
command = "npx"
args = ["chrome-devtools-mcp@latest"]
enabled = false
