interface:
  display_name: "Review"
  short_description: "Reviewer–Criticで証拠付きの指摘だけを検証"
  default_prompt: "Use $review to run a fresh-context Reviewer-Critic loop on the current diff, keep only evidence-grounded findings, and resolve accepted [must] findings before commit."

policy:
  allow_implicit_invocation: true
