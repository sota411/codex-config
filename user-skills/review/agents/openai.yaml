interface:
  display_name: "Review"
  short_description: "差分の証拠と要件から指摘の採否を判断し、必要な修正確認に絞る"
  default_prompt: "Use $review to review the current diff once, adjudicate findings against requirements and evidence, and report actionable issues with verification gaps."

policy:
  allow_implicit_invocation: true
