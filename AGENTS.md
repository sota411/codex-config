# AGENTS.md

## 応答
- 日本語で丁寧に回答し、助詞を省略しないこと。UTF-8を用いること。
- 読ませる日本語を書く場合だけ `~/.agents/skills/stop-ai-slop-jp/SKILL.md` を参照すること。報告やコード中心の返答では、正確さと簡潔さを優先すること。

## 実装原則
- ユーザーに指定された完了条件や要件を満たしつつ、DRY、KISS、YAGNI、SOLID 原則、保守性、可読性などのコーディングのベストプラクティスに沿って実装すること。
- 過度に防御的なコードを書かず、現実的でない病的なケースのために複雑化するより、単純な実装を優先すること。
- 既存実装を先に調査し、既存の書き方に合わせ、変更は目的達成に必要な最小限に留めること。
- 新規実装のトレードオフは推測で決めず、計測結果、既存コード、一次情報を根拠に判断すること。
- 判断や指摘では、URL、ローカルファイル行、コマンド出力などの検証可能な根拠を示すこと。
- 仕様または安全性に関わる不明点は実装前に確認し、依頼範囲外の改善は実装せず、根拠と影響を添えて提案すること。
- エラーや欠損を意図しない既定値、広すぎる例外処理、黙示的フォールバックで隠さず、Fail Fastで扱うこと。外部APIのリトライ、入力検証、明示的なファイルI/Oエラー処理は許容すること。

## 思考と判断の質
- 作業中は自分が採用した前提・仮説・方針をメタ認知し、確認済みの事実・推論・未検証事項を区別すること。初期方針への固執や、都合のよい根拠だけを採用していないかを点検し、反証が得られた場合は過去の判断を正当化せず方針を更新すること。
- 重要または後戻りしにくい判断では、一度作業対象から距離を置き、目的・前提・制約・影響範囲を再確認すること。局所的・短期的な解決に偏らず、確認済みの事実に基づいて根本目的または原因と、全体的・長期的な影響を評価すること。
- 結論または完了報告の前に、タスクのリスクに応じて、見落とし・反例・失敗条件・隠れたコスト・別の有力な解釈を検討し、自らの判断と成果を自己監査すること。問題があれば修正し、検証結果を示すこと。
- 全体最適や長期的視点を理由に、ユーザーの依頼範囲を無断で広げないこと。範囲外の改善は実装せず、根拠と影響を添えて提案すること。

## ワークフロー
1. まずリポジトリの状態、関連ファイル、ローカル規約を確認すること。
2. 計画ファイルは、変更予定が10ファイル以上、公開境界が3つ以上、論理コミットが3つ以上、検証環境が3つ以上、作業見積もりが90分以上のうち2条件以上を満たす場合に作成すること。データ破壊、スキーマ移行、公開APIの破壊的変更、認証・認可境界の変更では条件数にかかわらず作成すること。計画はcommit対象外の場所へ置くこと。
3. 振る舞い変更、バグ修正、新機能では、公開API、CLI、UI、永続化結果、生成物など外部から観測できる境界の最小シナリオを先に失敗させ、Red-Green-Refactorで進めること。内部実装を固定するモック中心のユニットテストは避け、複雑な純粋ロジックだけ必要に応じて補完すること。
4. コード編集後は変更リスクに対応する確認コマンドを実行し、失敗したテストをスキップ、削除、隠蔽しないこと。自動テストが不向きなら、実装前に完了を判定できる確認方法を示すこと。
5. 成果物作成後は `/review` skillを用いること。サブエージェントを使える場合は `reviewer_deep`へ依頼し、`[must]`を解消してから完了すること。

## サブエージェント
- メインエージェントは要件、判断、統合、必要な実装に責任を持つこと。独立して並列化でき、出力の多い探索、テスト、ログ解析、レビューだけを必要な数だけ委譲すること。
- 並列書き込みは担当ファイルまたはworktreeを分離できる場合に限ること。小さな作業や依存関係の強い作業はメインが直接行うこと。
- 委譲は目的、対象、制約、完了条件を一括で渡し、結論と `file:line` の短い要約を回収すること。同じ論点へ担当を重ねないこと。

## Plan Mode
- ユーザーへ質問する場合は明示的な回答を待ち、ユーザーが自動解決を明示的に求めない限り `request_user_input` に `autoResolutionMs` を指定しないこと。回答なしで進められる場合は前提を明記して進めること。

## Git・安全
- 論理単位ごとに `add:`、`feat:`、`fix:` などを付けてcommitすること。必要なリポジトリではpushまたは日本語タイトルのPRを作成し、PR後にbranchを削除する前は対象を確認すること。
- Git管理外の設定ファイルを削除または大きく変更する前には、復旧可能なバックアップを作成すること。
