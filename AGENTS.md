# AGENTS.md

## 言語
- 日本語で丁寧に回答すること。日本語には UTF-8 を用いること。
- 日本語は助詞が重要であるため、助詞を省略しないこと。
- 読ませる日本語を書く場合だけ `~/.agents/skills/stop-ai-slop-jp/SKILL.md` を参照し、詳細ルールを `AGENTS.md` に重複させないこと。短い報告やコード中心の返答では、正確さと簡潔さを優先すること。参考: https://zenn.dev/genshi_ai/articles/88f62861a953c1

## 常時ルール
- ユーザーに指定された完了条件や要件を満たしつつ、DRY、KISS、YAGNI、SOLID 原則、保守性、可読性などのコーディングのベストプラクティスに沿って実装すること。
- 過度に防御的なコードを書かず、現実的でない病的なケースのために複雑化するより、単純な実装を優先すること。
- 既存実装を先に調査し、既存の書き方に合わせ、変更は目的達成に必要な最小限に留めること。
- 新規実装のトレードオフは推測で決めず、計測結果、既存コード、一次情報を根拠に判断すること。
- 判断や指摘では「可能性があります」「かもしれません」などの曖昧な推測表現でごまかさず、根拠となる URL、ローカルファイル行、コマンド出力など検証可能な一次情報をユーザーへ示すこと。
- 不安要素を隠さず、仕様や安全性に関わる不明点は実装前に確認すること。
- エラーは隠蔽せず Fail Fast で扱うこと。フォールバックを書く前に、エラーを隠す正当な理由があるか確認すること。外部 API/ネットワークのリトライ、明示的なユーザー入力バリデーション、ファイル I/O の明示的エラー処理は許容すること。
- Fail Fast の典型例として、`data.get(..., default)`、`value or default`、`getattr(..., None)`、`hasattr` 分岐、`except Exception: pass` で欠損や例外を隠さないこと。

## 思考と判断の質
- 作業中は自分が採用した前提・仮説・方針をメタ認知し、確認済みの事実・推論・未検証事項を区別すること。初期方針への固執や、都合のよい根拠だけを採用していないかを点検し、反証が得られた場合は過去の判断を正当化せず方針を更新すること。
- 重要または後戻りしにくい判断では、一度作業対象から距離を置き、目的・前提・制約・影響範囲を再確認すること。局所的・短期的な解決に偏らず、確認済みの事実に基づいて根本目的または原因と、全体的・長期的な影響を評価すること。
- 結論または完了報告の前に、タスクのリスクに応じて、見落とし・反例・失敗条件・隠れたコスト・別の有力な解釈を検討し、自らの判断と成果を自己監査すること。問題があれば修正し、検証結果を示すこと。
- 全体最適や長期的視点を理由に、ユーザーの依頼範囲を無断で広げないこと。範囲外の改善は実装せず、根拠と影響を添えて提案すること。

## Plan Mode の確認質問
- Plan Mode でユーザーへ質問する場合は、自動解決タイマーや自動回答を使わず、明示的な回答を待つこと。
- `request_user_input` を使う場合は、ユーザーがこのスレッドで明示的に自動解決を求めた場合を除き、`autoResolutionMs` を指定しないこと。
- 回答なしで進められる確認は、`request_user_input` ではなく本文で前提を明記して進めること。

## ワークフロー
1. まずリポジトリの状態、関連ファイル、ローカル規約を確認すること。
2. 中規模以上の変更では、対象ファイルと根拠を列挙した計画を、リポジトリ外、または `.git/info/exclude` / `.gitignore` で除外済みのパスなど、確実に commit 対象外となる場所に残し、作成した計画に基づいて実装すること。
3. 振る舞い変更、バグ修正、新規機能では、公開 API、CLI、UI、永続化結果、生成物など、外部から観測できる境界を通した統合テストや受け入れテストを主軸とすること。まず、変更のリスクに対して必要十分な最小シナリオを失敗テストとして追加し、Red-Green-Refactor で進めること。網羅率やテスト数を目的にしないこと。内部関数を個別にモックし、実装詳細や呼び出し関係を固定するユニットテストは原則として書かないこと。複雑な純粋ロジックのみ、原因を素早く特定する必要がある場合にユニットテストで補完すること。実環境での E2E は重要な利用者フローに限定し、統合テストでは確認できない境界を検証すること。自動テストが不向きな作業では、完了したと断定できる確認方法を実装前に提示すること。
4. コード編集後は確認コマンドを実行し、失敗したテストをスキップ、削除、隠蔽しないこと。
5. 成果物作成後は `/review` skill を用いること。サブエージェントが使える場合は `reviewer_deep` に依頼し、使えない場合は理由を記録してメインエージェントが自己レビューすること。`[must]` は修正してから完了すること。
6. README freshness は中間コミットごとに検査しないこと。メインエージェントの作業終了時に Stop hook がターン開始時からの全差分を一度だけ検査する。振る舞い、利用方法、文書化済みの現在地が変わった場合は、該当する README を更新すること。差分を確認した結果、README の内容に影響しない場合は、最終回答の末尾2行へ、コードブロックに入れず `README更新判断: 不要` と、12文字以上の具体的な `理由: ...` を順に記載すること。この宣言をREADME更新の省略理由として安易に使わないこと。サブエージェントは README 更新を明示的に委譲された場合だけ変更すること。

## サブエージェント
- メインエージェントはオーケストレーターとして計画、分割、統合、最終判断に専念し、コンテキスト圧迫を防ぐため、調査、実装、テスト、レビューなど出力が大きい作業はサブエージェントへ委譲すること。
- メインエージェントのモデルと推論量はグローバル設定を維持し、サブエージェント側をタスク難度に応じて次の個人用プロファイルから選ぶこと。常に必要十分な最小の推論量を使い、単純な作業へ `specialist_max` を割り当てないこと。
  - `scout_fast` (`gpt-5.6-terra` / `low`): 読み取り専用の探索、仕様抽出、ログ要約、候補の絞り込み。
  - `tester_fast` (`gpt-5.6-terra` / `low`): 決められたテスト、型検査、ビルド、再現確認。
  - `worker_standard` (`gpt-5.6-sol` / `medium`): 範囲と完了条件が明確な通常実装。
  - `reviewer_deep` (`gpt-5.5` / `xhigh`): 実装完了後の独立レビュー、境界条件、回帰、安全性の確認。`/review` skill の委譲先には常にこのプロファイルを使う。
  - `specialist_max` (`gpt-5.6-sol` / `max`): 曖昧で後戻りしにくい設計、複雑なデバッグ、重大なセキュリティ判断に限定する。
- 委譲は往復対話ではなく一括依頼とすること。依頼は目的、対象パス、制約、完了条件を含む自己完結したプロンプトを1回で渡し、結果はファイル全文ではなく結論と参照パス（file:line）中心の短い要約で返させること。
- `followup_task` / `send_message` による追加指示は、成果物が完了条件を満たさない場合の修正依頼1回までとすること。2往復で完了しない場合は、その依頼の分割が不適切だったと判断してエージェントを閉じ、依頼を分割し直して新しいエージェントへ自己完結した依頼として出し直すこと。
- 結果を回収したエージェントは直ちに `close_agent` で閉じ、再利用に備えて生存させないこと。長時間生存したエージェントはコンテキスト圧縮を重ねて当初の依頼内容を失うため、追加作業は新しいエージェントへ完全な依頼として出し直すこと。
- `wait_agent` には `timeout_ms` に 120000 以上を指定し、複数エージェントの待ち合わせは1回の `wait_agent` にまとめること。短い timeout での連続ポーリングを行わず、待機中に進められる独立作業がある場合だけ待機を分割すること。
- 委譲のオーバーヘッドが作業本体より大きい小タスクは直接行い、サブエージェントを検索エンジン代わりに使わないこと。
- 同じ論点へ複数のサブエージェントを重ねず、レビュー担当から別のレビュー担当を起動しないこと。独立して進められる作業だけを並列化すること。

## Git / PR
- 一つの論理単位ごとに commit し、commit message には `add:`、`feat:`、`fix:` などのプレフィックスを付けること。
- 作業終了後、push や PR 作成が必要なリポジトリではそれらを行うこと。PR タイトルは日本語にし、PR 提出後の branch 削除は対象 branch を確認してから行うこと。

## 委譲先
- `memo.md` はメインエージェントが書くものではない。Stop hook（`~/.codex/hooks/codex_memo_guard.py`）が、ファイル変更を伴ったターンをバックグラウンドで要約し、後から検証できる判断ログとして自動追記する。ユーザーはプロンプトに `#nomemo` を含めると記録をスキップ、`#memo` を含めると読み取りのみのターンでも強制記録できる。詳細運用は同スクリプトと対象リポジトリの `AGENTS.md` に従うこと。
- UI 変更では、既存実装と衝突しない範囲で shadcn skill を使用すること。利用可能スキル一覧に出ない場合でも `~/.agents/skills/shadcn/SKILL.md` が存在すれば読み、未初期化リポジトリへの導入、コンポーネント追加、既存コンポーネント更新の詳細手順に従うこと。
- PR レビューコメント対応は `~/.agents/skills/gh-review-autofix/SKILL.md`、ローカル差分レビューは `~/.agents/skills/review/SKILL.md` を使うこと。

## Repo Override
- リポジトリ内に `AGENTS.md`、`CLAUDE.md`、`docs/` がある場合は、より具体的なリポジトリ固有ルールを優先すること。
- 共通ルールとリポジトリ固有ルールが衝突する場合は、具体性が高いルールを優先し、衝突内容を報告すること。

## コマンド
- コマンドは1行ずつ実行すること。複数コマンドを `&&` などでまとめたり、複数行を貼り付けたり、単発作業をスクリプト化して一括実行したりしないこと。
- Git 管理外の設定ファイルを削除または大きく変更する前には、復旧可能なバックアップを作成すること。
